## Dipendenze extra
Per avere i report Lighthouse installa Node.js e Lighthouse:

npm install -g lighthouse

per creare ambiente virtuale e installare Dipendenze:

python3 -m venv venv (oppure) python -m venv venv
source venv/bin/activate (oppure) venv\Scripts\activate

pip install -r requirements.txt
